% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CPM.R
\name{cpm.train.cv}
\alias{cpm.train.cv}
\title{cpm.train.cv}
\usage{
cpm.train.cv(data, outcome, p, nfolds = 5)
}
\arguments{
\item{data}{An N x E matrix containing the vectorized edges; where N = number of subjects, E=number of edges}

\item{outcome}{The outcome variable to predict}

\item{p}{a vector of p-values to tested. If \code{p} is not specified, an automatically determined geometric sequence of p-values will be tested}

\item{nfolds}{number of cross-validation folds. Set to 5 by default}
}
\value{
Returns a list object containing
\itemize{
\item \code{opt.pvals} A vector containing the 2 optimal p-values, one each for the positive and negative network models
\item \code{results} A P(number of p-values tested) x 2(positive and negative network models) matrix containing the predicted-actual correlations for each of the p-value selection thresholds in each network model.
\item \code{pvals} A vector of the p-value threshold tested
}
}
\description{
K-fold cross-validation procedure to determine the optimal p-value threshold for feature selection
}
\details{
This function runs the \code{cpm.train()} using a range of p-values and determines the optimal p-values that maximize predicted-actual correlation in a K-fold cross-validation paradigm.
}
\examples{
\dontrun{
model1.cv=cpm.train.cv(data=FC_data,outcome=dat_beh$age)
}
}
